<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/blu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/blp"
        android:supportsRtl="true"
        android:theme="@style/Theme._2312500602_RafifUAS"
        tools:targetApi="31">

        <!-- SplashActivity -->
        <activity
            android:name=".splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ResultBMI Activity -->
        <activity
            android:name=".ResultBMI"
            android:exported="false" />

        <!-- BMICalculator Activity -->
        <activity
            android:name=".BMICalculator"
            android:exported="false" />

        <!-- PenyakitActivity -->
        <activity
            android:name=".PenyakitActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>
